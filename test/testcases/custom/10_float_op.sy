int main() {
    int a, b;
    float fa, fb, fc;

    fa = 1.5;
    fb = 0.5;

    fc = fa + fb;
    putf("fc = %f\n", fc);

    fc = fa - fb;
    putf("fc = %f\n", fc);


    fc = fa * fb;
    putf("fc = %f\n", fc);

    fc = fa / fb;
    putf("fc = %f\n", fc);

    return 0;
}
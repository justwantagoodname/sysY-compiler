void func(){
    int i = 0;
    while(i < 10){
        putint(10);
        i = i + 1;
    }
}

int main() {
    int a = 1, b = 2, c = 3, d = 5, f;

    a = a * b * c + a * d + 4 + 5 * c * d;
    float dd = 0.5;

    int e[5][3];

    e[2][1] = 4;

    c = a + b;

    float g = 3.14;
    if(e[0][c])g = 12.6;
    if(a % 2  && b % 2 )
        printf("no");

    func();

    return 0;
}
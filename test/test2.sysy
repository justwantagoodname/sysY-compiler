void func(){
    int i = 0;
    while(i < 10){
        putint(10);
        i = i + 1;
    }
}

void func2(int a, int b, int c, int d, int e, int f, int g, int h, int i, int j) {
    int _a = a;
    int _b = b;
    int _c = c;
    int _d = d;
    int _e = e;
    int _f = f;
    int _g = g;
    int _h = h;
    int _i = i;
    int _j = j;
}

void func3(int a[]) {
    putf("%d\n", a[0]);
}

int main() {
    int a = 1, b = 2, c = 3, d = 5, f;

    a = a * b * c + a * d + 4 + 5 * c * d;
    float dd = 0.5;

    int e[5][3];

    e[2][1] = 4;

    c = a + b;

    float g = 3.14;
    if(e[0][c])g = 12.6;
    if(a % 2  && b % 2 )
        putf("no");

    func();

    putf("hello world! %f %f\n", 6.28, 3.14);

    int a[3] = {0, 1, 2};
    func3(a);

    return 0;
}